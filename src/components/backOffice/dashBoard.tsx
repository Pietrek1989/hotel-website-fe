const dashBoard = () => {
  //   const handleRefund = async (reservation) => {
  //     try {
  //       const response = await fetch(
  //         `${process.env.REACT_APP_BE_URL}/stripe/refund`,
  //         {
  //           method: "POST",
  //           headers: {
  //             "Content-Type": "application/json",
  //             Authorization: `Bearer ${getToken()}`, // include bearer token in the authorization header
  //           },
  //           body: JSON.stringify({
  //             reservationId: reservation._id,
  //             chargeId: reservation.content.chargeId, // Pass the chargeId here
  //           }),
  //         }
  //       );

  //       // ...
  //     } catch (error) {
  //       console.error(error);
  //     }
  //   };

  return (
    //     {!reservation.content.canceled && reservation.content.paid && (
    //         <button onClick={() => handleRefund(reservation)}>Refund</button>
    //       )}
    // )
    <div>hej</div>
  );
};

export default dashBoard;
